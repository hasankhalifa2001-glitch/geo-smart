import React from "react";

export default function MapCalculatorPage() {
    return (
        <div className="max-w-4xl mx-auto space-y-6">
            <h1 className="text-3xl font-bold">Map Calculator</h1>
            <p className="text-slate-600 dark:text-zinc-400">
                Plot property boundaries on the interactive map to calculate areas, perimeters, and generate reports.
            </p>
        </div>
    );
}
